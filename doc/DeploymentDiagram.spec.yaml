meta:
  profile: engineering-review
  theme: tech-blue
  source: generated
  layout: hierarchical
  title: Diagrama de Despliegue - Microservicios Gimnasio
  description: >-
    Cada nodo de ejecucion aloja un artefacto Spring Boot Jar (JRE 17) y su instancia H2 embebida en memoria.
    clase-service consume entrenador-service via REST/HTTP.
  legend: >-
    Nodo = entorno de ejecucion (JVM / contenedor). Artifact = jar desplegado. Flecha solida = llamada REST sincrona
    (HTTP).
modules:
  - id: node_clase
    label: '«device» Nodo :: clase-microservice (:8080)'
  - id: node_entrenador
    label: '«device» Nodo :: entrenador-microservice (:8081)'
  - id: node_equipo
    label: '«device» Nodo :: equipo-microservice (:8082)'
  - id: node_miembro
    label: '«device» Nodo :: miembro-microservice (:8083)'
nodes:
  - id: jar_clase
    label: |-
      «artifact» clase-service.jar
      (Spring Boot)
    type: service
    module: node_clase
  - id: db_clase
    label: H2 (in-memory)
    type: database
    module: node_clase
  - id: jar_entrenador
    label: |-
      «artifact» entrenador-service.jar
      (Spring Boot)
    type: service
    module: node_entrenador
  - id: db_entrenador
    label: H2 (in-memory)
    type: database
    module: node_entrenador
  - id: jar_equipo
    label: |-
      «artifact» equipo-service.jar
      (Spring Boot)
    type: service
    module: node_equipo
  - id: db_equipo
    label: H2 (in-memory)
    type: database
    module: node_equipo
  - id: jar_miembro
    label: |-
      «artifact» miembro-service.jar
      (Spring Boot)
    type: service
    module: node_miembro
  - id: db_miembro
    label: H2 (in-memory)
    type: database
    module: node_miembro
edges:
  - from: jar_clase
    to: db_clase
    type: data
  - from: jar_entrenador
    to: db_entrenador
    type: data
  - from: jar_equipo
    to: db_equipo
    type: data
  - from: jar_miembro
    to: db_miembro
    type: data
  - from: jar_clase
    to: jar_entrenador
    type: primary
    label: «HTTP/REST» GET /api/gimnasio/entrenadores/{id}
